# Generate this machine-specific section with: node scripts/setup.mjs
# Copy the resulting generated/codex.toml into your Codex configuration.
# No author-specific executable or directory paths are shipped.
